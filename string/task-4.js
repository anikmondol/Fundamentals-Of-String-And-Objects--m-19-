// If a given string has either x, replace x by y. if the given string has X, replace it by Y.


const string = 'If a given string has either x, replace x by y. if the given string has X, replace it by Y.'


console.log(string);

// for(const num of task4){
   
// }

const newString = string.replace('x', 'y');
// console.log(newString);

const and = newString.replace("X", 'Y');
console.log(and);